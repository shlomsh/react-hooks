import { useEffect } from "react";

/**
 * useDocumentTitle — synchronize the browser tab title with a string.
 *
 * TODO: implement this hook.
 *
 * Requirements:
 *  1. Use useEffect to set document.title = title after every render where title changes.
 *  2. Provide a dependency array: [title]
 *  3. Return a cleanup function that resets the title to "" when the component unmounts.
 */
export function useDocumentTitle(title: string): void {
  // TODO: write your useEffect here
}
